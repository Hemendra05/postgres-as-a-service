standby_mode = 'on'
primary_conninfo = 'host={{ hostvars['primary']['ansible_host'] }} port=5432 user={{ replication_user }} password={{ replication_password }}'
trigger_file = '/tmp/postgresql.trigger.5432'
